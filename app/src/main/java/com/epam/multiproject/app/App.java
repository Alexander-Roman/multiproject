/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.epam.multiproject.app;

import com.epam.multiproject.list.LinkedList;

import static com.epam.multiproject.utilities.StringUtils.join;
import static com.epam.multiproject.utilities.StringUtils.split;
import static com.epam.multiproject.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
